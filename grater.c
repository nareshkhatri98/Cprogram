#include<stdio.h>
int Grater(int,int);
int main()
{
    int num1,num2,a,b;
    printf("enter the first and second nummber:");
    scanf("%d%d",&num1,&num2);
    a=Greater(num1,num2);
    =Grater(a,b);
    printf("grater is=%d",y);
    return 0;
}
int Grater(int x ,int y)
{
  if(x>y)
  {
    return x;
  }
  else{
    return y;
  }
}